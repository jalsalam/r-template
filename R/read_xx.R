# read_xx.R
# Reads input data from xx

read_xx_table <- function(table) {
  #function defines how to read a single table
  
}

read_xx_set <- function(set_of_tables) {
  #function calls read_xx_table for each member of set_of_tables
  
}